import React from "react";
import { StyleSheet, Text, View } from "react-native";

const swap = () => {
  return (
    <View>
      <Text>swap</Text>
    </View>
  );
};

export default swap;

const styles = StyleSheet.create({});
